<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_settings"
    app:startDestination="@id/setting_fragment">

    <fragment
        android:id="@+id/setting_fragment"
        android:name="com.example.falldetection.ui.settings.SettingFragment"
        android:label="@string/title_setting"
        tools:layout="@layout/fragment_setting" />
    <action
        android:id="@+id/action_global_settingFragment"
        app:destination="@id/setting_fragment"
        app:popUpTo="@id/setting_fragment" />
</navigation>